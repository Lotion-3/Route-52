RAW_recipes.csv
backend/recipes_raw.jsonl
backend/recipes_tagged_staging.jsonl
backend/recipes_ready_to_merge.jsonl

# Environment variables
.env
.env.local
.env.*.local
config.env

# Session caches — contain LIVE auth cookies (__Host-instacart_sid et al).
# NEVER commit. Glob-matched rather than listed one path at a time: the old
# list named `backend/aldi/.aldi_session.json` while the file actually written
# is `backend/.aldi_session.json`, so real Instacart cookies for ALDI and King
# Soopers were committed and are still in git history.
*_session.json
.*_session.json
**/.ic_session.json
**/.aldi_session.json
**/.ks_session.json
**/.walmart_http_session.json
**/.target_http_session.json
**/.coles_http_session.json
**/.woolworths_http_session.json

# Python
__pycache__
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
.venv
pip-log.txt
.coverage
*.log

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Project specific
.cache/
archive/
RAW_recipes.csv
node_modules/
.next/
dist/
build/
backend/meals_curated.jsonl
